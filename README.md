
1. **路由层（api）不写业务逻辑**
2. **业务逻辑只写 services**
3. **DB 连接与表结构只写 db**
4. **schema 是接口契约，字段变更必须同步改 schema**
5. **core 放工具函数，别把工具散到各文件里**

---
提交规范

1. 有新的依赖及时写到requirement.txt当中
2. 过程性文件和不必要的视频丢到.gitgnore中