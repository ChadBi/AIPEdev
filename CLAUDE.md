# AIPE指令

每一次任务完成之后都需要在progress.md文件中标注本次任务干了什么，修改了哪些文件，有没有潜在的问题

## 沟通规范

- 所有回复、解释、提问一律使用中文
- 代码注释使用中文，变量/函数/类命名使用英文
- 每执行一个步骤前，先用一句话告诉我在做什么，完成后简短汇报结果

## 工作风格

- 边做边汇报，不要默默执行一大堆操作后才说话
- 遇到不确定的地方，列出 2-3 个方案并说明各自优缺点，让我来决定
- 任务完成后给出简短总结：做了什么、改了哪些文件、有没有遗留问题

## 编码规范

- 写最简单能跑的代码，不过度设计
- 不要删除我的文件，不要做我没要求的重构
- 优先用现成的库，不重复造轮子
- 不要留下调试用的 print/console.log，用 logger 代替
- 不要添加 `# type: ignore`，有类型问题就修根本原因

## 技术栈偏好

- Python：优先用 

  ```
  uv run
  ```

   执行，包管理用 uv，如果当前工作区没有虚拟环境，就用 

  ```
  uv venv
  ```

   创建一个

  - **永远不改动 base/全局环境**：禁止用 `pip install`（不带虚拟环境时）、`uv tool install` 往全局装包
  - 所有依赖必须安装到项目的虚拟环境（`.venv`）中，用 `uv add` 管理
  - 安装包前先确认当前激活的是项目虚拟环境，可用 `uv run python -c "import sys; print(sys.prefix)"` 验证
  - 若检测到没有虚拟环境，先执行 `uv venv`，再执行 `uv sync`，**不要**直接裸跑 `pip install`

- 爬虫：优先 httpx + parsel，复杂交互才用 playwright

- 前端：优先原生或轻量方案，非必要不引入大框架

- AI/ML：优先用现成 API（如 OpenAI、Anthropic），非必要不自己训练

## Git 提交规范

- 每完成一个独立步骤，自动执行 `git add` 和 `git commit`
- 提交前告诉我提交信息是什么，确认后再提交
- 提交信息格式：`类型: 简短描述`，例如 `feat: 添加用户登录接口`
- 类型只用这几种：feat（新功能）、fix（修bug）、refactor（重构）、chore（杂项）
- 不要 `git push`，除非我明确要求

## 安全底线

- 不执行任何删除文件/目录的命令
- 不修改 `.env` 文件的内容
- 不推送代码到远程仓库，除非我明确要求
- **不污染 Python base 环境**：任何 `pip install` 或包安装操作，必须在项目虚拟环境内执行

## 认知流程

1. **理解**：先确认我的需求，不确定就问
2. **计划**：说明打算怎么做
3. **执行**：逐步操作，每步汇报
4. **验证**：运行测试或检查输出，确认结果正确
